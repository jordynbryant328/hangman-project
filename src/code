class main(){
  method(addLeg) {}
  method(addBody) {}
  method(addArm) {}
  method(addHead) {}
}
class Arm() {
  line = x,y;
}
class Leg() {
  line = x,y;
}
class Body() {
  line = x,y;
}
class Head() {
  circle(x,y,width,height);
}
class Letters() {
 println("" + Array[1] + "");
}
class Word() {
  *read from file*
  if(userGuess != Array[1]) {
  addLeg
  } else if (userGuess = Array[1]) {
  if(userGuess != Array[2]) {}
